# example of overriding an existing property
debug: true

oauths:
  github:
    # example of overriding non-existing nested property
    callback: http://testing.acme.com/oauth/github/callback 
  google:
    # example of overriding existing nested property
    callback: http://testing.acme.com/oauth/google/callback

